package com.sass;

/**
 * An enum of CSS output styles.
 */
public enum OutputStyle {
    /** Each selector and declaration is written on its own line. */
    EXPANDED,

    /** CSS is written with as few characters as possible. */
    COMPRESSED
}
